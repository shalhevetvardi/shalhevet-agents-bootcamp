# HANDOFF / STATE - rebuild-plugin (סקיל התלמידים)

**עודכן:** 19-09-2026 | **גרסה חיה:** v0.4.3 | **ריפו:** `shalhevet-agents-bootcamp`

מסמך זה מתעד את מצב סקיל ה-rebuild של תלמידי הטירונות, כדי שאפשר להמשיך ממנו בכל חשבון/מחשב. הוא עצמאי - הכל כאן.

---

## מה זה
`rebuild-plugin` = סקיל שתלמידי טירונות מתקינים, שאורז מחדש את הסוכן שלהם לקובץ `.plugin`, **מתקין אותו אוטומטית ומחליף את הגרסה הישנה**. חי ב-`skills/rebuild-plugin/` בריפו הזה, ומופץ לתלמידים דרך קישור הורדה מ-GitHub.

## הגרסה החיה: v0.4.3 (18-09-2026)
- **מה היא עושה:** אריזה + התקנה אוטומטית + החלפת הישן, **בבטחה**: גיבוי לפני החלפה, אימות אחרי, שחזור-אוטומטי בכישלון (ואז נפילה לידני), 4 בדיקות מבנה לפני אריזה (שורש הפלאגין, מיקום SKILL.md, BOM, סכמת plugin.json), **רישום מלא** של 3 קבצי הרישום (installed_plugins + marketplace.json + enabledPlugins) כך שהפלאגין באמת עולה, אריזת Windows דרך `System.IO.Compression` (לא `Compress-Archive`), `robocopy` לפלאגינים כבדים, דילוג בחן אם אין תיקיית Claude Desktop.
- **מבוססת** על הגרסה של התלמיד **משה** (kontakt@drehpunkt-ab.com), שנבדקה על **Windows 11 אמיתי**.
- **commit:** מיזוג PR #1 - `422cced` על `main` (18-09-2026, 18:52 שעון ישראל). אומת חי: הקישור הציבורי מגיש zip שה-`plugin.json` שבתוכו בגרסה 0.4.3.
- **קישור ההורדה (מה שהתלמידים מקבלים):**
  `https://github.com/shalhevetvardi/shalhevet-agents-bootcamp/raw/main/skills/rebuild-plugin.zip`
- **מייל broadcast** (בשחרור v0.4.0): "ה-rebuild תוקן - התקינו את הגרסה המעודכנת" נשלח/מוכן ל-45 תלמידים פעילים (דרך סקיל `kaas:broadcast`). ל-v0.4.3 לא נשלח broadcast (תיקון מטא-דאטה, הקישור לא השתנה). הוכרע ב-19-09-2026 (שלהבת, בשיחה): אין צורך להודיע לשאר התלמידים.

## הסאגה (איך הגענו ל-v0.4.0)
1. **v0.2.0 (03/08)** - הוסיף auto-install; **שבר תלמידי Windows**: PowerShell `Out-File` הוסיף BOM לקבצי הרישום ← מנתח ה-JSON נפל ← כל הפלאגינים המותקנים נעלמו בשקט.
2. **v0.3.0 (05/08)** - נסיגה: ירד ל-**התקנה ידנית בטוחה** (הסקיל לא כותב לקבצים הפנימיים), כדי לעצור את הדימום.
3. **v0.4.0 (07/08)** - הפתרון האופטימלי: משה שלח SKILL.md שעושה auto-install **בבטחה** (הבאג של ה-BOM תוקן + רשת ביטחון: גיבוי/אימות/שחזור). אומץ ושוחרר.
4. **v0.4.1 (08/08)** - התלמידה **ליטל** בדקה על כל 24 הסוכנים שלה (Windows) ומצאה שההתקנה מתקינה קבצים אבל **לא רושמת את הפלאגין עד הסוף** (חסרו `marketplace.json` ו-`enabledPlugins`, אז הפלאגין נראה מותקן ולא עלה). תוקן: רישום מלא של 3 קבצי הרישום + אימות 4-מישורים + בדיקת סכמת plugin.json + תיקון נתיב-ארוך ב-Windows.
5. **v0.4.2 (08/08)** - ליטל אישרה ש-v0.4.1 עובד (הסוכנים עולים), ומצאה נקודה מינורית: האורז כלל קבצי `.plugin`/`.zip` ישנים מתיקיית הסוכן. תוקן: החרגת `*.plugin`/`*.zip` באריזה. (נקודת ה"נייד / חלון Plugins" נדחתה - לא רלוונטית לתלמידים שעובדים ב-Claude Code.)
6. **v0.4.3 (18-09)** - התלמיד **אלי** דיווח (מייל 15/09, סעיף 3, מסומן חוסם) ש-v0.4.2 "נדחה בוולידציה של קלוד": `description` ב-`plugin.json` היה 541 תווים (עברית+אנגלית כפולה) מול "תקרה 500" - כך לפי דיווחו; הוא לא צירף את הודעת השגיאה ולא ציין באיזה ממשק (Cowork מוזכר אצלו רק בסעיף 4). תיעוד קלוד הרשמי ל-`plugin.json` (manifest-reference.md) אינו מצטט תקרה, אלא ממליץ 50-200 תווים - כלומר 500 הוא מספר מדווח ולא מתועד, והסיבתיות (האורך גרם לדחייה) לא שוחזרה בהתקנה אמיתית. קוצר ל-304 תווים (נוסח אחיד עברית+אנגלית קצר, בלי לאבד עובדה מהותית - 4 בדיקות מבנה, רישום מלא, גיבוי+אימות+שחזור, cross-OS), הגרסה עלתה ל-0.4.3, וה-zip נבנה מחדש. **נוסף שומר קבוע** - `scripts/check_plugin_descriptions.py` (עם `--self-test` שמוכיח את עצמו מול המחרוזת בת 541 התווים שבאמת שברה התקנה) - שיתפוס `description` חורג מ-500 תווים בכל `plugin.json` בריפו, ומונע חזרה על התקלה בסקילים עתידיים. **טרם אומת:** שההתקנה אצל אלי עוברת עכשיו - ממתין לתשובתו.

## מדריך ההתקנה בנושן (סעיף 4 במייל של אלי) - מצב והמשך

**מה תוקן (18-09-2026):** בדף הנושן "Plugins - איך מתקינים, משתמשים ובונים מחדש" (מזהה `37b13b4c-3d78-8142-b7c1-e55337a7ffd8`) שתי שורות. התקנה: Customize ← Plugins ← Add ▾ ← Upload a plugin (במקום Settings ← Plugins ← Install from file, שלא קיים ב-Cowork). הסרה: Customize ← Plugins, פותחים את הפלאגין ולוחצים Uninstall. **המקור הוא הדף בנושן** - לא לערוך את העותק ב-`kaas-mcp-server` (`knowledge/agents/bnaya/guides-content/`), הוא נדרס בסנכרון האוטומטי (כל 6 שעות). נמדד ב-19-09: העותק כבר התעדכן.

**על מה זה נשען:** התיעוד הרשמי של Cowork (Customize ← Plugins, ואפשרות העלאה בעמוד Plugins) והדיווח של אלי. הניסוח המדויק "Add ▾ ← Upload a plugin" הוא של אלי - התיעוד הרשמי אומר רק "upload option".

**עותקים של הנתיב הישן שנמצאו ב-19-09 (נמדד עם `git grep` על main של כל ריפו):** בידע של יוסף (ריפו `sales-automation`), בכרטיס ההתקנה של סוכן בגלריה (ריפו `studio-gallery`), ובקובץ הפגישה החמישית של בְּנָיָה (ריפואים `kaas-mcp-server` ו-`Storyteling`). הנתיבים המדויקים ברשומת ה-VO של הסקיל מ-19-09. **לפני שמכריזים שסעיף 4 סגור** - להריץ בכל אחד מהם `git grep -n -e "Install from file" -e "Settings → Plugins" origin/main`.

**מה שהפקודה הזו מחזירה ב-19-09, אחרי מיזוג ה-PR-ים של בְּנָיָה (נמדד):** ב-`Storyteling` - כלום. ב-`kaas-mcp-server` - שורה אחת, והיא לא המדריך: מוקאפ של מסך "איך מתקינים" במפרט פורטל KaaS מזכיר נתיב Add Local (נוסח אחר; לא נבדק אם הנתיב קיים, ו-Add Local לא מופיע בשום קובץ אחר ב-`main` של הריפו). ב-`sales-automation` - שתי שורות, שתיהן בקובץ המדריך של יוסף (הצ'יפ שלמטה). ב-`studio-gallery` - 11: שתיים ברכיב הכרטיס (הערה ושורת הטקסט החיה, הצ'יפ שלמטה) ותשע בקבצי `.planning/` של הריפו. **הערכה, לא נבדקה:** תשע אלה רשומות היסטוריות של תוכניות עבודה ולא טקסט שהתלמיד רואה, ולכן לא עורכים אותן. כשהצ'יפים של יוסף והגלריה מסתיימים, מה שנשאר בפקודה הוא רק המוקאפ ב-`kaas-mcp-server` ותשע שורות ה-`.planning/`.

| נושא | מי אחראי |
|---|---|
| קטע ה-Rebuild באותו מדריך נראה מיושן: הוא מתאר מחיקה והתקנה ידניות, והסקיל עושה זאת לבד מגרסה 0.4.0 | צ'יפ שהתחיל ב-19-09 - לבדוק ולהציע לשלהבת, בלי לערוך בלי אישור |
| העתק המדריך בידע של יוסף | צ'יפ - רענון לפי הנוהל של יוסף, PR בלי מיזוג |
| הנוסח בכרטיס ההתקנה של סוכן בגלריה | צ'יפ - החלטת נוסח של שלהבת, PR בלי מיזוג |
| השורה בקובץ הפגישה החמישית של בְּנָיָה | תוקנה ב-19-09 על ידי הסשן שעובד על שאר הסעיפים במייל של אלי, בשני PR-ים שמוזגו ל-`main` באותו יום: Storyteling #306 ו-kaas-mcp-server #146 (נמדד ב-19-09 בנתיב הישיר של כל PR: `merged: true`). נמדד גם ב-`main` של שני הריפו: בקובץ הפגישה שורה 84 אומרת Customize, והנוסח הישן Install from file לא מופיע. לפי הסשן ההוא (נמדד אצלו): המקור של הקובץ ב-Storyteling והוא עובר לשרת דרך kaas-sync, לא מנושן |
| מוקאפ מסך ההוראות במפרט פורטל KaaS (ריפו `kaas-mcp-server`, נוסח Add Local) | לא הואצל: מסמך מפרט. לא נבדק אם הפורטל עצמו מציג את הנוסח הזה, ולא נבדק אם הנתיב קיים בקלוד. שלהבת מחליטה אם לבדוק |
| תיעוד פנימי מ-05/2026 בנושן ("בְּנָיָה - תיעוד הסוכן") | לא הואצל: תיעוד מערכת היסטורי, לא הוראה לתלמיד |
| אישור מאלי שההתקנה עוברת | שלהבת. נמדד ב-19-09: בג'ימייל ממתינות שתי טיוטות בשרשור של אלי (מ-18-09 ומ-19-09), ואף אחת מהן לא נשלחה. לבחור אחת לפני שליחה |

**נמדד ב-19-09 על הגבול של 500 תווים:** לפלאגין פנימי אחד שלך ב-Storyteling יש תיאור של 981 תווים, והוא מותקן וטוען כרגיל. כלומר טעינה לא נכשלת על אורך. ההשערה (לא נבדקה): ההגבלה חלה רק בהעלאה ידנית. השומר בריפו הזה בודק רק את המניפסטים שבו.

## קבצים (נתיבים מלאים)
- `skills/rebuild-plugin/SKILL.md` - הלוגיקה (v0.4.3). **מקור-האמת - עורכים כאן.**
- `skills/rebuild-plugin/.claude-plugin/plugin.json` - version 0.4.3.
- `skills/rebuild-plugin/README.md` - הסבר לתלמיד.
- `skills/rebuild-plugin.zip` - חבילת ההפצה (מה שהקישור מגיש). מכיל את התיקייה `rebuild-plugin/` עם 3 הקבצים.
- `scripts/check_plugin_descriptions.py` - השומר: נכשל אם `description` של `plugin.json` כלשהו בריפו עובר 500 תווים. `--self-test` מוכיח שהוא לא ריק. רץ אוטומטית ב-`.github/workflows/check-plugin-manifests.yml` כשמשתנה `plugin.json` או הסקריפט.
- `skills/CONTRIBUTING.md` - צ'קליסט הפרסום. הסעיף "עדכון סקיל קיים" כולל עכשיו את השומר ואת אימות ההורדה הציבורית.

## אם צריך להמשיך לתקן (v0.4.1+)
1. לערוך את `skills/rebuild-plugin/SKILL.md` (המקור).
2. להעלות את `version` ב-`skills/rebuild-plugin/.claude-plugin/plugin.json`.
3. **לבנות מחדש את ה-zip** (חובה, אחרת הקישור מגיש גרסה ישנה):
   `cd skills && zip -rq rebuild-plugin.zip rebuild-plugin -x '*.DS_Store'`
4. לאמת: אין BOM ב-plugin.json (`od -An -tx1 -N3 <plugin.json>` צריך `7b ...`, לא `ef bb bf`), JSON תקין, וה-zip מכיל את המבנה `rebuild-plugin/`.
5. commit ממוקד עם **pathspec מפורש** (לא `git add -A`) + `git push`. הקישור מגיש מ-`main` מיד.
6. אם משנים התנהגות מהותית - מייל broadcast לתלמידים (סקיל `kaas:broadcast`, ל-45 הפעילים).

## כללי בטיחות (למה להיזהר - 4 מלכודות שבירה-בשקט)
1. **רישום מלא = 4 מישורים** (v0.4.1). פלאגין עולה רק אם: (א) הקבצים בתיקייה, (ב) רשומה ב-`marketplace.json`, (ג) רשומה ב-`installed_plugins.json`, (ד) `enabledPlugins: true` ב-`settings.json`. כתיבה לכולם - רק בבטחה: בלי BOM, קריאה-קודם + שימור רשומות, גיבוי+אימות+שחזור, דילוג אם אין Claude Desktop. (v0.4.0 פספס ב+ד ← הפלאגין נראה מותקן ולא עלה.)
2. **BOM = רוצח JSON שקט** (PowerShell `Out-File` מוסיף אותו).
3. **Windows: `System.IO.Compression`, לא `Compress-Archive`** (backslash שובר התקנה).
4. **`SKILL.md` תמיד ב-`skills/<שם>/SKILL.md`** (בשורש = הסוכן לא מופיע, בלי שגיאה).

## תיעוד ב-VO (Virtual Office)
- ישות סקיל: «rebuild-plugin (סקיל תלמידים)» (מחלקת KaaS) - עם ADR מלא של הסאגה ב-history.
- כלל: «kaas-plugin-packaging-safety» (מחלקת KaaS) - 4 המלכודות.

## אסור לגעת
- **עבודת המחזורים** (Cohort/backfill) - של הסשן "אנה", לא קשור לזה.
- **הפלאגין הפנימי של KaaS** ב-Storyteling - הוחלט מפורשות לא לערוך (המלכודות לא נוגעות בו).

## הקשר סשן (דברים אחרים שקרו 06-07/08, לא חלק מה-rebuild)
- CLAUDE.md / Starter Rules של הטירונות - עברו התאמה גנרית לכל מערכת הפעלה (חי בנושן + בריפו).
- באג טירגוט המחזורים בברודקאסט - הועבר לטיפול הסשן "אנה".
